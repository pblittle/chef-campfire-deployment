site :opscode

metadata

group :integration do
  cookbook 'apt'
  cookbook 'yum'
  cookbook 'campfire-deployment-test', :path => 'test/cookbooks/campfire-deployment-test'
  cookbook 'minitest-handler', :github => 'btm/minitest-handler-cookbook'
end
